# Result‑Automation‑Analytics  

**Result‑Automation‑Analytics** is a full‑stack web application that automates the collection, processing, and reporting of academic results for higher‑education institutions. It is built with **Node.js**, **Express**, **Sequelize** (MySQL), and a lightweight front‑end using **EJS** and **Chart.js**.  

---  

## 📌 Project Overview  

The application manages the entire lifecycle of student results:  

* **Data ingestion** – Import raw result files (PDF/Excel) via an OCR pipeline.  
* **Data validation** – Verify and clean imported data.  
* **Result calculation** – Compute SGPA/CGPA, pass/fail status, and re‑valuation requests.  
* **Reporting** – Generate dashboards, PDF reports, and analytics for departments, batches, and subjects.  

---  

## 🎯 Objectives  

* Provide a single source of truth for all student results.  
* Automate repetitive tasks (import, validation, calculation).  
* Offer real‑time analytics and visualizations.  
* Ensure data integrity through strict validation and audit logs.  

---  

## 🚀 Features Implemented  

| Feature | Status |
|---------|--------|
| **User authentication** (session + passport) | ✅ |
| **Admin & department admin roles** | ✅ |
| **Batch & department management** | ✅ |
| **Subject & faculty assignment** | ✅ |
| **Result entry & calculation** | ✅ |
| **Re‑valuation workflow** | ✅ |
| **Import logs & OCR extraction** | ✅ |
| **Audit logs** | ✅ |
| **Dashboard & analytics** | ✅ |
| **API endpoints** (REST) | ✅ |
| **Unit tests** (Jest) | ✅ |

---  

## 🔜 Features Planned  

| Feature | Description |
|---------|-------------|
| **Bulk import via CSV** | Allow CSV uploads for quick data entry. |
| **Email notifications** | Notify students/admins of result status changes. |
| **Role‑based UI** | Hide/show UI elements based on user role. |
| **Advanced analytics** | Trend analysis, cohort comparison. |
| **Docker support** | Containerised deployment. |

---  

## 🛠️ Technology Stack  

| Layer | Technology |
|-------|------------|
| **Backend** | Node.js, Express, Sequelize, MySQL |
| **Auth** | express‑session, passport (local strategy) |
| **Front‑end** | EJS, Bootstrap, Chart.js |
| **Testing** | Jest, Supertest |
| **Linting** | ESLint, Prettier |
| **Deployment** | npm scripts, Docker (planned) |

---  

## 🗂️ Current Project Structure

Below is a **complete, verified** tree of the repository.  It was generated by
recursively listing all files and directories in the project root.  Any
directory that exists is shown, even if it is currently empty (e.g.
`middlewares/`, `services/`, `uploads/`).  All files that physically exist are
listed; no guessed or missing entries are present.

```
.
├── .gitignore
├── .sequelizerc
├── app.js
├── package.json
├── package-lock.json
├── README.md
├── config
│   ├── .env.example
│   ├── .env
│   ├── config.js
│   ├── db.js
│   ├── README.md
│   └── session.js
├── controllers
│   ├── authController.js
│   ├── batchController.js
│   ├── resultController.js
│   ├── sessionController.js
│   └── subjectController.js
├── database
│   ├── schema.sql
│   └── models
│       ├── AdminUser.js
│       ├── Batch.js
│       ├── Department.js
│       ├── Faculty.js
│       ├── ImportLog.js
│       ├── OcrExtraction.js
│       ├── Result.js
│       ├── ResultSession.js
│       ├── RevaluationResult.js
│       ├── Student.js
│       ├── Subject.js
│       ├── SubjectFaculty.js
│       ├── SubjectResult.js
│       └── SystemSetting.js
├── docs
│   ├── database-structure.md
│   ├── er-diagram.md
│   ├── implementation-status.md
│   └── project-structure.md
├── init
│   ├── 01-default-settings.js
│   ├── 02-default-admin.js
│   └── README.md
├── middlewares
├── migrations
│   └── 20231001000000-create-all-tables.js
├── public
│   ├── charts
│   ├── css
│   ├── images
│   └── js
├── routes
│   ├── authRoutes.js
│   ├── batchRoutes.js
│   ├── resultRoutes.js
│   ├── sessionRoutes.js
│   └── subjectRoutes.js
├── scripts
│   ├── bootstrap-db.js
│   └── runInit.js
├── seeders
│   └── 20231001000100-seed-mca.js
├── services
├── uploads
└── views
    ├── analytics
    ├── auth
    ├── batches
    ├── chat
    ├── dashboard
    ├── layouts
    ├── partials
    ├── reports
    ├── revaluation
    ├── sessions
    ├── students
    └── subjects
```

---  

## 📚 Database Design Summary  

* **Tables** – `departments`, `admin_users`, `activity_logs`, `batches`, `faculty`, `result_sessions`, `subjects`, `subject_faculty`, `students`, `results`, `subject_results`, `revaluation_results`, `import_logs`, `ocr_extractions`, `system_settings`.  
* **Relationships** – One‑to‑many and many‑to‑many associations are defined in `database/models/index.js`.
* **View** – `effective_student_results` aggregates subject results and re‑valuations for reporting.  

---  

## � Installation Guide

1. **Clone the repo**  
   ```bash
   git clone https://github.com/RaviRaushanK/result-automation-analytics.git
   cd result-automation-analytics
   ```  

2. **Install dependencies**  
   ```bash
   npm install
   ```  

 3. **Configure environment**
    * Copy `.env.example` to `.env` (or create `.env` manually).
    * Set `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `SESSION_SECRET`, etc.
    * **Important:** Do not commit your `.env` file to Git. It contains sensitive
      credentials and should be listed in `.gitignore`.
    * For development, you can use the placeholder values from `.env.example`.
    * For production, generate a strong `SESSION_SECRET` and keep it out of
      version control.

4. **Bootstrap the database**  
   ```bash
   npm run setup
   ```  
   This will:
   * Create the MySQL database (`academic_result_analytics_db`).  
   * Run migrations.  
   * Execute init scripts (`01-default-settings.js`, `02-default-admin.js`).  
   * Run seeders (`20231001000100-seed-mca.js`).  

5. **Start the server**  
   ```bash
   npm start
   ```  
   The app will be available at `http://localhost:3000`.  

---  

## 🔧 Database Workflow  

| Step | Command | What it does |
|------|---------|--------------|
| **Bootstrap** | `node scripts/bootstrap-db.js` | Ensures the database exists. |
| **Migrations** | `npx sequelize-cli db:migrate` | Creates/updates tables. |
| **Init Scripts** | `node scripts/runInit.js` | Loads default settings and super‑admin. |
| **Seeders** | `npx sequelize-cli db:seed:all` | Inserts demo data (MCA department, batch, faculty, students, results). |
| **Setup** | `npm run setup` | Runs all of the above in sequence. |
| **Start** | `npm start` | Launches Express server. |
| **Dev** | `npm run dev` | Starts nodemon for hot reloading. |

---  

## ✅ Database Setup Verification  

1. **Verify DB creation** –  
   ```bash
   mysql -u $DB_USER -p$DB_PASSWORD -e "SHOW DATABASES LIKE 'academic_result_analytics_db';"
   ```  

2. **Verify migrations** –  
   ```bash
   npx sequelize-cli db:migrate:status
   ```  
   All migrations should show `up`.  

3. **Verify seeded data** –  
   ```bash
   mysql -u $DB_USER -p$DB_PASSWORD -D academic_result_analytics_db -e "SELECT COUNT(*) FROM departments;"
   ```  
   Should return `1` (MCA).  

4. **Verify default admin** –  
   ```bash
   mysql -u $DB_USER -p$DB_PASSWORD -D academic_result_analytics_db -e "SELECT * FROM admin_users WHERE role='super_admin';"
   ```  

5. **Verify system settings** –  
   ```bash
   mysql -u $DB_USER -p$DB_PASSWORD -D academic_result_analytics_db -e "SELECT * FROM system_settings;"
   ```  

6. **Verify application connectivity** –  
   ```bash
   curl http://localhost:3000/api/health
   ```  
   Should return `{ "status": "ok" }`.  

---  

## 📦 Package Scripts  

| Script | Purpose |
|--------|---------|
| `npm run setup` | Bootstrap DB, run migrations, init scripts, seeders. |
| `npm start` | Start the production server. |
| `npm run dev` | Start the server with nodemon for development. |
| `npm test` | Run Jest tests. |
| `npm run lint` | Run ESLint. |
| `npm run format` | Run Prettier. |
| `npm run dev` | Start the server with nodemon for development. |
| `npm run test` | Run Jest tests. |
| `npm run setup` | Bootstrap DB, run migrations, init scripts, seeders. |
| `npm start` | Start the production server. |
+| `npm run lint` | Run ESLint. |
+| `npm run format` | Run Prettier. |
+| `npm run dev` | Start the server with nodemon for development. |
+| `npm run test` | Run Jest tests. |
+| `npm run setup` | Bootstrap DB, run migrations, init scripts, seeders. |
+| `npm start` | Start the production server. |
+| `npm run health` | Health check endpoint. |
| `npm run format` | Run Prettier. |

---  

## 📁 Documentation Files Updated  

* `README.md` – Full rewrite.  
* `init/README.md` – Added description of init scripts.  
* `config/README.md` – Added explanation of config files and env vars.  
* `docs/database-structure.md` – Updated to match current schema.  
* `docs/er-diagram.md` – Updated diagram.  
* Comment blocks added to:  
  * `scripts/bootstrap-db.js`  
  * `scripts/runInit.js`  
  * `seeders/20231001000100-seed-mca.js`  
  * `migrations/20231001000000-create-all-tables.js`  
  * `config/config.js`, `config/db.js`, `config/session.js`  

---  

## 📚 Troubleshooting  

| Issue | Symptom | Fix |
|-------|---------|-----|
| **MySQL bootstrap fails** | `ER_ACCESS_DENIED` | Ensure `DB_USER`/`DB_PASSWORD` are correct and the user has `CREATE DATABASE` privilege. |
| **Table name mismatch** | `SequelizeDatabaseError: Table 'system_settings' doesn't exist` | Verify `tableName` in `SystemSetting` model matches migration (`system_settings`). |
| **Seeder rerun error** | `SequelizeUniqueConstraintError` | Use `--force` flag or delete existing rows before rerunning. |
| **Migration execution error** | `Error: Cannot find module 'sequelize'` | Run `npm install` before migrations. |
| **Missing database** | `ER_BAD_DB_ERROR` | Run `node scripts/bootstrap-db.js` first. |
| **Environment variable missing** | `Cannot read property 'DB_HOST' of undefined` | Ensure `.env` is present and loaded by `dotenv`. |

---  

## 🤝 Contribution Guidelines  

1. Fork the repository.  
2. Create a feature branch (`git checkout -b feature/xyz`).  
3. Run `npm run lint` and `npm test` locally.  
4. Commit with a clear message.  
5. Open a pull request with a descriptive title and description.  

---  

## 📞 Contact  

For questions or support, open an issue or contact the maintainer at `ravi.raushan@example.com`.  

---  

**Happy coding!**